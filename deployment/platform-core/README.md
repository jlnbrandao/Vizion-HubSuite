# Platform Core (Hub) — the existing Vizion stack.

# From the repository root:
#   docker compose up --build
#
# This file is a pointer. The canonical Hub compose remains at the repo root
# so current deployments keep working without a path change.
