[project]
name = "openvizion-iot"
version = "0.1.0"
description = "OpenVizion IoT — distributable product scaffold (no domain yet)."
requires-python = ">=3.13"
dependencies = [
    "openvizion-kernel",
    "openvizion-observability",
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "pydantic>=2.10.0",
    "pydantic-settings>=2.6.0",
    "httpx>=0.28.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/iot"]

[tool.hatch.build.targets.wheel.force-include]
"src/iot" = "iot"

[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["src", "../../../packages/kernel/src", "../../../packages/observability/src"]
testpaths = ["tests"]
